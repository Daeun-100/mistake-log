{
  "compilerOptions": {
    "target": "ES6",
    "jsx": "react-jsx",
    "module": "ES6", // 컴파일 후 생성될 파일이 사용하는 모듈 버전
    "outDir": "./dist", // 컴파일된 파일은 dist 폴더에 저장
    "rootDir": "./src", // 소스 파일은 src 폴더 기준
    "skipLibCheck": true,
    "esModuleInterop": true,
    "moduleResolution": "node",
    "types": ["jest"]
  },
  "include": ["src/**/*.tsx", "src/**/*.ts"], // src 폴더의 모든 파일 포함
  "exclude": ["node_modules", "dist"]
}
